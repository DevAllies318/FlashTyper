<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= error ? "Server Error" : "Page Not Found" %></title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f8f8f8;
        color: #333;
        text-align: center;
        padding: 50px;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #fff;
        padding: 30px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      h1 {
        font-size: 48px;
        margin: 0;
      }
      p {
        font-size: 18px;
        color: #666;
      }
      a {
        color: #3498db;
        text-decoration: none;
        font-weight: bold;
      }
      a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1><%= error ? "500 - Server Error" : "404 - Page Not Found" %></h1>
      <p>
        <% if (error) { %> An unexpected error occurred on the server. Please
        try again later. <% } else { %> The page you're looking for doesn't
        exist or may have been moved. <% } %>
      </p>
      <p>
        <a href="/">Go back to Home</a>
      </p>
      <% if (error) { %>
      <hr />
      <p style="color: red; font-size: 14px"><%= error.message %></p>
      <pre style="text-align: left; font-size: 12px"><%= error.stack %></pre>
      <% } %>
    </div>
  </body>
</html>
